{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'php artisan key:generate --force && php artisan storage:link && php artisan migrate:fresh --force --seed && php artisan serve --host 0.0.0.0 --port $PORT'"
  }
}

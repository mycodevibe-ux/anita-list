{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "sh -c 'cp -f .env.example .env && php artisan key:generate --force && php artisan config:clear && php artisan storage:link && php artisan filament:assets && php artisan migrate:fresh --force --seed && php artisan serve --host 0.0.0.0 --port $PORT'"
  }
}
